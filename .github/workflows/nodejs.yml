name: make lint
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    strategy:
      # Node versions list
      matrix:
        node-version: [v17.8.0]

    steps:
      - name: my-step
        run: make lint
